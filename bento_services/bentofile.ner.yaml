# bento_services/bentofile.ner.yaml

service: "bento_services.ner_service:NERService"

labels:
  owner: waseem
  project: medical-analyzer

include:
  - "pyproject.toml"
  - "uv.lock"
  - "bento_services/**"
  - "app/**"

python:
  packages:
    - bentoml
    - gliner
    - torch
    - logfire
    - pydantic